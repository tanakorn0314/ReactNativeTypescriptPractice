import { StyleSheet } from 'react-native';

export const viewPagerStyles = StyleSheet.create({
    container: {
        backgroundColor: 'red'
    },
    contentContainer: {
        justifyContent: 'center',
        alignItems: 'center'
    }
});